@using KJ.Tourify.WebUI.Models.Entities
@model ICollection<GalleryCategory>

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Gallery Categories</h5>
        <a class="btn btn-primary float-end" asp-action="Create">Add Gallery Category</a>
    </div>
    <div class="card-body">

        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Category</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var galleryCategory in Model)
                {
                    <tr>
                        <td>@galleryCategory.Name</td>
                        <td>
                            <button class="btn btn-danger" onclick='Delete("@(galleryCategory.Id)", this)'>Delete</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>


@section PageScripts{
        <script>
            function Delete(id, elem){
                $.ajax({
                    url: "/Management/GalleryCategories/Delete/" + id,
                    method: "POST",
                    success: function(s){
                       if(s.success == true){
                           $(elem).closest("tr").remove();
                       }
                       else{
                           alert(s.message);
                       }
                    },
                    error: function(){
                        alert("Bir sorun oluştu!");
                    }
                })
            }
        </script>
}
